# 模组崩溃诊断

*Exported on 2026年8月2日 20:37:58*

**Assistant**:

> 我们被提供了一个日志文件，分析 Minecraft 启动崩溃。日志显示是 Zalith Launcher (移动端) 启动 26.2 Fabric 版本。从日志末端可以看到完整的崩溃报告，描述为 "Initializing game"，根本原因是 mixin 注入失败。> 关键信息：> - Minecraft 版本: 26.2 (对应新版本号方案，2026 年，需要 Java 25)> - 加载器: Fabric 0.19.3> - 启用模组: 63 个，包括 fabric-api, iris, sodium, lithium, ferritecore, quirky, placeholder-api, reeses-sodium-options, sodium-extra, modmenu, cloth-config 等。> - 崩溃发生在渲染线程初始化阶段，具体是在 ParticleResources 注册时，因为 quirky 模组的 mixin 注入失败。> - 崩溃报告中的异常链: BootstrapMethodError -> RuntimeException(Mixin transformation of net.minecraft.client.particle.SingleQuadParticle failed) -> MixinTransformerError -> MixinApplyError(Mixin [quirky.client.mixins.json:FlameParticleMixin from mod quirky] FAILED during APPLY) -> InvalidInjectionException(描述符不匹配)。> - 详细错误：quirky 模组的 `FlameParticleMixin` 尝试注入到 `SingleQuadParticle` 的构造函数，但期望的参数列表是 `(ClientLevel, double, double, double, TextureAtlasSprite, CallbackInfo)`，但实际找到的是 `(ClientLevel, double, double, double, double, double, double, TextureAtlasSprite, CallbackInfo)`。说明 quirky 模组与 Minecraft 26.2 版本不兼容，因为 Minecraft 26.2 中 `SingleQuadParticle` 构造函数的参数发生了变化（可能增加了速度参数）。> 结论：已确认，根因是模组 `quirky` 版本 0.1.0 与 Minecraft 26.2 不兼容，其 mixin 注入目标方法签名不匹配，导致游戏初始化失败。> 环境信息：> - 运行环境：客户端> - 平台：Android (vivo V2425A, API 36)> - 启动器：Zalith Launcher 2.4.10> - 渲染器：MobileGlues (OpenGL 4.0)> - Minecraft 版本：26.2> - 加载器：Fabric 0.19.3> - Java 版本：OpenJDK 25.0.3-internal (64-bit)> - 内存：-Xms4711M -Xmx4711M (约 4.7GB)> - 关键模组：quirky 0.1.0 (导致崩溃)> 解决方案：移除 quirky 模组，或联系作者更新兼容 26.2 的版本。> 其他潜在问题：日志中还有关于 cacio agent 的 ClassNotFoundException 警告，但似乎没有影响启动，因为游戏继续进行直到崩溃。另外有 `WARNING: Unknown module: net.fabricmc.loader.impl.launch.knot specified to --add-exports` 和 `WARNING: package sun.security.action not in java.base`，这些是 Java 25 的兼容性问题，但非致命。还有 `Sodium has applied one or more workarounds` 和 `Could not find any graphics adapters`，但这是 Android 移动端正常现象，没有致命。> 输出格式：按照要求，第一部分环境总结，第二部分主要问题诊断（已确认），第三部分解决方案，第四部分其他潜在问题。

**第一部分：环境总结**

- 运行环境：客户端
- 平台：Android 移动端（vivo V2425A，API 36）
- 启动器：Zalith Launcher 2.4.10
- 渲染器：MobileGlues（OpenGL 4.0，OpenGL ES 3.2）
- Minecraft 版本：26.2
- 加载器：Fabric 0.19.3
- Java 版本：OpenJDK 64-Bit Server VM 25.0.3-internal
- 内存分配：`-Xms4711M -Xmx4711M`
- 与根因直接相关的模组：`quirky` 0.1.0

**第二部分：主要崩溃原因诊断**

- **结论状态：已确认**
- 游戏初始化阶段因模组 `quirky` 的 Mixin 注入失败而导致进程终止。
- 崩溃报告 `Description: Initializing game` 及异常链明确指向 `MixinApplyError`：`quirky.client.mixins.json:FlameParticleMixin from mod quirky` 在对 `net.minecraft.client.particle.SingleQuadParticle` 进行 `@Inject` 注入时，因**方法描述符不匹配**而失败。Mixin 期望的构造函数参数为 `(ClientLevel, double, double, double, TextureAtlasSprite, CallbackInfo)`，但 Minecraft 26.2 中实际参数为 `(ClientLevel, double, double, double, double, double, double, TextureAtlasSprite, CallbackInfo)`（包含速度参数）。
- 该错误直接导致 `SingleQuadParticle` 类无法被 Mixin 转换，从而在 `ParticleResources.registerProviders` 阶段抛出 `BootstrapMethodError`，游戏崩溃。
- 根因为 `quirky` 0.1.0 版本与 Minecraft 26.2 不兼容，未适配 26.2 中 `SingleQuadParticle` 构造函数的签名变更。

**第三部分：针对崩溃的解决方案**

- 移除模组 `quirky`（0.1.0），或将其替换为支持 Minecraft 26.2 的更新版本（如有）。
- 如果希望保留 `quirky` 的功能，请联系模组作者确认是否有针对 26.2 的更新，或等待兼容版本发布后再重新加入。

**第四部分：其他潜在问题**

- **Java Agent 兼容性警告**：日志中出现 `WARNING: Unknown module: net.fabricmc.loader.impl.launch.knot specified to --add-exports` 和 `WARNING: package sun.security.action not in java.base`，以及 Cacio 代理的 `ClassNotFoundException: sun/java2d/SurfaceManagerFactory`。这些是 Java 25 对旧版 Cacio 组件的不兼容警告，但并未阻止游戏启动至崩溃点，因此不是本次崩溃的直接原因。若后续遇到与图形界面或 AWT 相关的异常，可考虑更新 Cacio 组件或联系启动器开发者。
- **操作系统信息获取失败**：多处 `File not found or not readable` 及 `AccessDeniedException`（如 `/proc/stat`、`/sys/devices/...`）是 Android 移动端正常权限限制，不影响游戏运行。

---
